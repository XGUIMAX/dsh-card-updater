# 权限与运行边界

这个插件要动用户的卡片文件、要联网、要在用户点击时调用系统的文件管理器打开目录。这些能力在源码里都是可见的信号，这里把每一条讲清楚：它做什么用、什么时候发生、失败时会怎样。

DSH STORE 的目录检查从 manifest、README 与源码提取信号，本文覆盖它列出的五类：文件、网络、命令、凭据、可执行产物。

## 依赖

**运行时零依赖。** 宿主端只 import Node 内置模块（`node:fs`、`node:path`、`node:crypto`、`node:child_process`、`node:url`、`node:os`）与 Host 注入的服务（`ctx.webServer`、`ctx.slots`、`ctx.locale`、`ctx.effect`）。没有 `dependencies`，没有构建步骤，没有原生模块。

`peerDependencies` 声明 `@deepseek-ai/cordis ^4.0.1`，由 DSH 本身提供。

`engines.node` 为 `>=18`：宿主端只用得到该版本已有的 API，没有用到任何更新的语法或内置能力。

DSH 兼容范围声明在 `package.json` 的 `dsh.compatibility` 里。`dshReleases` 只标出实际验证过的版本，其余留空由目录自行判定，不做没验证过的承诺。

## 文件访问

**读：**

| 位置 | 用途 | 时机 |
| --- | --- | --- |
| Tavern 的卡片目录 | 读取人物卡本体、生成头像缩略图 | 打开面板、检测、合并 |
| Tavern 的 `data/index.json` 与 `data/sessions.json` | 读取卡片工作台对话的绑定、时间与打开记录 | 打开面板时（结果缓存 30 秒） |
| Tavern 的 `data/chats/*/journals/*.jsonl` | 取对话最近写入时间，判断调试是否进行中 | 打开面板时（同上缓存） |
| 本插件自己的数据目录 | 配置、上次检测报告、备份清单、头像缓存 | 全程 |
| 用户在选择目录时停留的那个文件夹，以及本机的盘符 | 列出下一层子目录供选择；盘符表由 26 次 `existsSync` 探测得出，不列驱动器内容 | 仅在用户打开目录选择器时，每次只读一层 |

**写：**

| 位置 | 用途 | 时机 |
| --- | --- | --- |
| 卡片文件 | 更新原版卡、导入新版、合并进 MVU 版 | **仅在用户点击对应按钮时**，且写入前先备份 |
| 本插件自己的备份目录（默认在数据目录下，可在面板里换到任意文件夹） | 每次写卡前的快照 | 同上，自动 |
| 本插件自己的数据目录 | 配置与状态 | 用户保存设置时 |
| 头像缓存 | 缩略图 | 后台预热，失败不影响功能 |

写入范围**只限于**上述位置。插件不写 Tavern 的工作台对话、不写它的索引，那些数据只读。

## 网络访问

| 目标 | 用途 | 触发方式 |
| --- | --- | --- |
| `api.github.com` | 检查本插件是否有新版本 | 打开面板时自动一次（2 分钟缓存），或点击「检测更新」 |
| `forum.shimmerday.top` | 社区索引站：按卡名检索原贴、读取贴子是否有更新 | 检测卡片时 |
| 卡片配置的来源链接 | 下载新版卡文件 | 点击「更新原版」或「更新并合并」时 |
| `github.com` | 用户点击「打开仓库」时由浏览器访问，插件自身不请求 | 手动 |

**没有遥测，没有回传。** 插件不向任何服务器报告使用情况、卡片内容或本机路径。

## 命令执行

宿主端有三处调用外部命令，都在受控范围内：

**打开文件夹**：用户点击「打开目录」时，交给系统自己的文件管理器打开一个已存在的目录。macOS 用 `open`，Linux 用 `xdg-open`，两者都以数组形式传参，不经过 shell。Windows 用 `cmd.exe /c start "" <目录>`：Windows 上直接 `explorer.exe <目录>` 会打开一个**永远不显示**的窗口（实测某个备份目录累计 31 个同名窗口、全部不可见，所以按钮"没反应"），而 `start` 把路径交给 shell 时窗口是正常的。路径仍是单独的 argv 元素、由 Node 加引号，目录名里的空格和 `&` 留在引号内。

**选择文件夹**：用户点击「选择备份目录」时，宿主端先向宿主自己的 `directoryPicker` 服务要一个路径。用系统对话框还是宿主自带的浏览器由宿主决定，对话框进程也由宿主启动，插件只接收结果。宿主没有这个服务、或它自己报错时，退回面板内的目录浏览器，并把**原因**写在界面上 —— 早先的版本在这里静默退化，看起来像"点了没反应"。

**取插件自身版本 / 自更新**：`git` 读取远端版本、`git pull --ff-only`，仅在用户点击对应按钮时。

**这是全部。** 早期版本在 Windows 上还会调用 PowerShell 与 System.Drawing 生成 96px 头像缩略图。那个调用需要 `-ExecutionPolicy Bypass` 和运行时拼接的命令串，被终端防护读成了脚本启动器（火绒报 `TrojanDownloader/JS.Agent.is`）。缩略图只是省一点带宽，而样式表里的 `object-fit: cover` 本来就会缩放，所以那段代码已经删除：头像现在直接返回原图，由浏览器缩放。

除上述之外，宿主端不执行任何外部程序。

## 凭据

插件保存**一个**凭据：社区索引站的 `auth_token`。

- 由用户手动从索引站页面取得后粘贴进来，插件不代为登录
- 存在本插件自己的 `config.json` 里，`0o600` 权限
- **只发给 `forum.shimmerday.top` 一个域名**，作为 `Authorization: Bearer` 头
- 插件从不读取浏览器 cookie、环境变量里的其他凭据、SSH 密钥或系统钥匙串
- 令牌有 7 天有效期，过期后插件会明确提示并给出重新获取的入口

用户不配置该令牌时，卡片检测仍可用，只是无法检索 Discord 社区贴子。插件会在卡片上说明这一点，不会因为缺少凭据而失败。

## 可执行产物

仓库里带四个安装脚本：`install.ps1`、`uninstall.ps1`、`install.sh`、`uninstall.sh`。

它们**不是**运行时的一部分，宿主端代码不会调用它们。它们只在用户手动执行时运行，做三件事：把插件以 `link:` 形式写进某个 profile 的依赖、把包名追加进该 profile 的 bundle 列表、在 profile 目录执行一次 `pnpm install`。

脚本的标准替代方式是 DSH 自带的 `dsh plugin --profile <name> add`，效果相同且更权威。脚本存在的意义是留给没有 `dsh` 命令的场景，以及希望在改动 profile 清单前先留一份备份的场景。

## 外部服务与失败边界

**索引站 `forum.shimmerday.top` 不可达**：卡片上提示检查代理，并说明插件运行在 Node 进程里、不读系统代理设置，需要 TUN 模式。检测本身仍然完成，只是社区检索那部分标为失败。不会阻塞其他卡片。

**GitHub 不可达**：更新检查报告失败，面板其余功能照常。不会影响卡片检测。

**卡片来源挂着密码、权限或赞助**：下载会失败。检测阶段就已经记下了这些条件，所以失败时给的是「此人物卡来源无法直接更新，请访问原贴下载新卡后导入」，而不是原始的网络错误。手动下载后走「导入新版」仍然可行。

**卡文件格式不对**：导入前先解析确认那确实是一张人物卡，不通过就拒绝，**现有卡不动**。

**合并时两边内容不一致**：保留用户本地改过的版本，并在合并报告里列出来，不静默覆盖。

**任何写卡操作之前都会先备份。** 备份按文件保留最近 5 份、总量上限 1000 份，超出自动清理。用户可以从面板回滚到任意一份。

**检测到有卡正在调试时，写卡操作会被拒绝**，并说明原因：卡片 Agent 自己持有一份卡并在收工时写回，这时从面板改同一个文件会互相覆盖。

## 安装、启动与卸载

以下步骤在一次性 profile 上实测过，无需人工审核回复即可复现。

**安装：**

```sh
# 在一个新的 profile 上安装
dsh plugin --profile <profile> add github:XGUIMAX/dsh-card-updater
```

安装后检查该 profile 的 `package.json`：`dependencies` 中应出现 `dsh-card-updater`，`dsh.profile.bundles` 中应出现同名条目。`dsh plugin` 在装完后会自动核对 bundle 列表，声明了 `dsh.bundle` 的依赖会自行进入层栈，不需要手工改清单。

**启动：** 重启 DSH。侧栏底部出现「卡片更新器」入口，设置里出现同名分区。打开面板应当读到卡片列表；若未安装 DSH Tavern，面板会直接说明并给出安装地址，而不是显示空列表。

**卸载：**

```sh
dsh plugin --profile <profile> remove dsh-card-updater
```

卸载后该 profile 的 `dependencies` 与 `dsh.profile.bundles` 都应不再包含本插件，且**其余依赖与 bundle 条目保持不变**。插件的数据目录（配置、备份、头像缓存）会保留，需要清理时手动删除 `~/.dsh/profile-data/<profile>/data/tools/card-updater`。

实测记录：在临时 profile 上完成安装与卸载往返后，逐个比对该 profile 及其他 profile 的清单哈希，除目标 profile 外均未变化。

## 报告问题

权限范围、外部服务或失败行为与此处描述不符，属于缺陷，请提到 <https://github.com/XGUIMAX/dsh-card-updater/issues>。
